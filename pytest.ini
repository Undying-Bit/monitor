[pytest]
testpaths = tests
norecursedirs = tmp .pytest-tmp databases logs .venv
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers
markers =
    asyncio: mark a test as asyncio-based
filterwarnings =
    error::pytest.PytestUnhandledCoroutineWarning
